{
  "name": "saas-erb-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "db:push": "cd apps/backend && supabase db push",
    "db:pull": "cd apps/backend && supabase db pull"
  },
  "devDependencies": {
    "turbo": "^2.8.3"
  }
}
